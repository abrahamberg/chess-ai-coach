import type { ReactNode } from 'react';

/** Placeholder — GET /api/lichess/recent-games doesn't exist until Task 7.1
 * (Lichess integration). Wired up to real data there. */
export function LichessGamePicker(): ReactNode {
  return <p>Importing from Lichess is coming soon. For now, paste your PGN instead.</p>;
}
